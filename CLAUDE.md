프로젝트 명 : AFTERWAVE
오늘 지구가 남긴 가장 큰 신호

# T04 — Today’s Seismic Signal

## 1. 프로젝트 목표

USGS 공개 데이터를 이용해 조회 시점 기준 최근 24시간 동안 발생한 지진 중 규모가 가장 큰 지진을 표시하고, Asia/Seoul 기준으로 하루 한 건을 기록한다.

사용자는 다음 정보를 한 화면에서 확인할 수 있어야 한다.

* 최근 24시간 최대 지진 규모와 단위
* 발생 위치와 깊이
* 지진 발생 시각
* USGS 원천 데이터 생성 시각
* 서비스 조회 시각
* 기준 시간대
* 공개 원천 URL
* 전날 기록 대비 규모 변화
* 현재 데이터가 최신인지, 오래된 값인지, 실패 상태인지

앱은 데이터 수신에 실패해도 마지막 정상값을 삭제하지 않고 정직하게 현재 상태를 설명해야 한다.

---

## 2. 기술 원칙

* 기존 프로젝트의 프레임워크와 패키지 구성을 우선 사용한다.
* 신규 프로젝트라면 Next.js App Router, TypeScript를 사용한다.
* 실제 데이터는 USGS GeoJSON 공개 피드에서 가져온다.
* 기본 원천 URL:
  `https://earthquake.usgs.gov/earthquakes/feed/v1.0/summary/all_day.geojson`
* API 키가 필요한 외부 데이터 원천을 추가하지 않는다.
* 외부 데이터 호출과 DB 저장은 서버 경로에서 처리한다.
* 비밀값은 서버 환경변수에서만 사용하고 클라이언트 번들, 응답, 로그, Git 기록에 노출하지 않는다.
* `fetch`에는 캐시 정책을 명시하고 실제 조회에서는 오래된 정적 응답을 사용하지 않는다.
* 외부 응답은 TypeScript 타입만 신뢰하지 말고 런타임에서 구조를 검사한다.
* 과제용 asset의 package ID, SHA-256, fixture 값은 직접 만들어내지 말고 제공된 파일에서 읽는다.
* 기존 코드와 사용자 변경사항을 보존하고 관련 없는 파일은 수정하지 않는다.

---

## 3. 실제 데이터 정의

대표값은 다음과 같이 정의한다.

> 조회 완료 시점을 기준으로 최근 24시간 USGS 피드에 포함된 지진 중 `properties.mag`가 가장 큰 지진의 규모

“오늘 발생한 지진”이라고 표현하지 않는다. USGS 피드는 고정된 KST 날짜 범위가 아니라 최근 24시간 범위이기 때문이다.

동일 규모가 여러 건이면 다음 규칙을 적용한다.

1. 규모가 큰 지진
2. 규모가 같으면 발생 시각이 최근인 지진
3. 그래도 같으면 지진 ID의 오름차순

정규화 결과에는 최소한 다음 필드를 포함한다.

* `earthquakeId`
* `magnitude`
* `unit: "M"`
* `place`
* `depthKm`
* `latitude`
* `longitude`
* `observedAt`: 지진 발생 시각
* `sourceUpdatedAt`: 해당 지진 자료 수정 시각
* `sourceGeneratedAt`: USGS 피드 생성 시각
* `requestedAt`: 서비스 조회 완료 시각
* `recordDate`: `Asia/Seoul` 기준 `YYYY-MM-DD`
* `sourceUrl`: 해당 지진 상세 페이지
* `feedUrl`: 조회한 GeoJSON 주소

시간값은 내부적으로 ISO 8601 또는 epoch millisecond로 보존하고 화면에서 `Asia/Seoul`로 변환한다.

---

## 4. 일별 저장 규칙

* `recordDate`를 일별 고유키로 사용한다.
* 날짜 키는 반드시 `Asia/Seoul` 기준으로 서버에서 생성한다.
* 같은 KST 날짜에 여러 번 성공하면 새로운 행을 추가하지 않고 기존 한 건을 갱신한다.
* 다음 KST 날짜에 성공하면 새로운 행을 정확히 한 건 추가한다.
* 실제 기록과 합성 시험 기록은 서로 다른 저장소 또는 명확히 분리된 namespace를 사용한다.
* 합성 fixture가 실제 일별 기록 2건을 대신해서는 안 된다.
* 실제 기록이 아직 2건이 아니라면 데이터를 조작하지 않고 “다음 실제 날짜 기록 대기 중”이라고 표시한다.
* 공개 심사 화면에서는 실제 기록이 서로 다른 KST 날짜 기준 정확히 2건임을 확인할 수 있어야 한다.

어제 대비 변화는 저장된 최근 두 실제 기록을 날짜순으로 정렬한 뒤 다음과 같이 계산한다.

`오늘 규모 - 이전 기록 규모`

소수점 오차를 제어하고 화면 표시 규칙과 재계산 규칙을 동일하게 사용한다.

---

## 5. 실패 및 복구 규칙

다음 다섯 실패를 제공된 합성 fixture로 각각 재생한다.

* 느린 응답 또는 timeout
* 외부 원천 401/403
* 호출 제한 429
* 오프라인 또는 네트워크 단절
* 응답 스키마 변경

각 실패는 서로 다른 제목, 설명, 오류 코드, 사용자 행동을 제공한다. 모든 실패를 동일한 “오류가 발생했습니다” 문구로 처리하지 않는다.

실패 시:

* 마지막 정상값을 삭제하지 않는다.
* 마지막 정상값에 `stale` 상태와 “마지막 정상 확인값” 표시를 붙인다.
* 마지막 성공 시각을 표시한다.
* 실패 원인을 사람이 이해할 수 있는 문장으로 표시한다.
* 다시 시도 버튼을 제공한다.
* 합성 실패는 실제 USGS 요청을 발생시키지 않는다.
* 합성 실패는 실제 일별 기록을 변경하지 않는다.

공개 asset의 `T04-RECOVER-D2`를 재생하면:

* 상태가 `fresh`로 복구된다.
* `error_code`가 `none`이 된다.
* 합성 저장소에 다음 날짜 기록이 정확히 한 건 추가된다.
* 마지막 정상값과 변화값이 복구 fixture를 기준으로 다시 계산된다.

---

## 6. 디자인 방향

전체 디자인은 iPhone 위젯에서 영감을 받은 글라스모피즘 대시보드로 만든다. Apple UI를 그대로 복제하지 않고 프로젝트만의 지진파 시각 언어를 사용한다.

### 시각 스타일

* 어두운 청회색 또는 검푸른 배경 위에 낮은 채도의 오로라 그라디언트를 사용한다.
* 배경에는 매우 약한 지진파 동심원 또는 진폭 그래픽을 배치한다.
* 카드에는 반투명 배경, backdrop blur, 얇은 밝은 테두리, 부드러운 그림자를 사용한다.
* 주요 카드 radius는 24~32px 범위로 통일한다.
* 유리 효과는 장식으로만 사용하고 텍스트 뒤의 명암 대비를 충분히 확보한다.
* 과도한 네온, 강한 광택, 여러 색상의 무분별한 혼합은 피한다.
* 지진 규모 숫자가 화면에서 가장 높은 시각적 우선순위를 가져야 한다.
* 아이콘은 단순한 선형 아이콘 또는 CSS 기반 지진파 그래픽을 사용한다.
* 실제 Apple 로고, SF Symbols 파일 또는 Apple 자산을 복제하지 않는다.

### 권장 레이아웃

1. 상단: 서비스명, 현재 상태 배지, 마지막 조회 시각
2. 메인 위젯: 최대 규모, 단위, 발생 위치, 어제 대비
3. 상세 위젯: 깊이, 좌표, 발생 시각, 원천 생성 시각, 기준 시간대
4. 기록 위젯: 서로 다른 KST 날짜의 실제 기록 2건
5. 신뢰 정보 위젯: 원자료·저장값·화면값 비교
6. 실패 실험실: 다섯 실패와 복구 fixture를 재생하는 버튼
7. 출처 영역: USGS 상세 페이지와 원본 피드 링크

모바일에서는 한 열, 넓은 화면에서는 12-column grid를 사용한다. 1366×768에서도 대표값, 상태, 출처, 두 시각이 스크롤 없이 확인되도록 우선 배치한다.

---

## 7. 접근성 및 상태 표현

* 본문 텍스트는 최소 16px을 권장한다.
* 상태는 색상만으로 표현하지 않고 아이콘과 텍스트를 함께 사용한다.
* 키보드만으로 모든 버튼과 링크에 접근할 수 있어야 한다.
* 포커스 스타일을 제거하지 않는다.
* `prefers-reduced-motion`에서는 지진파 애니메이션을 중지하거나 축소한다.
* `prefers-reduced-transparency` 또는 blur 미지원 환경을 위한 불투명 배경 fallback을 둔다.
* 로딩 중에는 기존 정상값을 숨기지 않고 조회 중 상태를 함께 표시한다.
* 오류 메시지는 화면에 지속적으로 표시하고 자동으로 사라지게 하지 않는다.
* 작은 회색 글씨로 출처나 stale 상태를 숨기지 않는다.

---

## 8. 금지 사항

* 실제 지진 데이터를 fixture처럼 하드코딩하지 않는다.
* 서로 다른 실제 날짜의 기록을 임의 생성하지 않는다.
* 합성 데이터를 실제 데이터처럼 표시하지 않는다.
* 발생 시각, 원천 생성 시각, 조회 시각을 하나의 시각으로 합치지 않는다.
* localStorage만으로 실제 일별 기록을 보존하지 않는다.
* 오류 발생 시 `0`, `-`, 임의 기본값을 실제 값처럼 표시하지 않는다.
* 실패 후 마지막 정상값을 삭제하거나 빈 화면으로 교체하지 않는다.
* 지도 라이브러리를 핵심 요구사항보다 먼저 구현하지 않는다.
* 글라스 효과 때문에 가독성과 상태 설명을 희생하지 않는다.
* 과제에서 제공하지 않은 package ID, SHA-256 또는 fixture 내용을 추측하지 않는다.

---

## 9. 완료 기준

작업 완료 전 다음을 검증한다.

* 공개 화면이 로그인 없이 열린다.
* 실제 USGS 값과 단위가 표시된다.
* 출처 URL, 발생 시각, 원천 생성 시각, 조회 시각, Asia/Seoul이 표시된다.
* 원자료·저장값·화면값이 일치한다.
* 같은 KST 날짜의 반복 성공이 한 행으로 유지된다.
* 다음 KST 날짜의 성공이 새 행을 만든다.
* 실제 날짜 기록 2건과 변화값이 보인다.
* 다섯 실패가 서로 다르게 표시된다.
* 실패 후 마지막 정상값과 stale 표시가 남는다.
* 다시 시도와 `T04-RECOVER-D2` 복구가 동작한다.
* 합성 시험이 실제 기록을 변경하지 않는다.
* 브라우저, 네트워크 응답, 배포 파일, 저장소에 비밀값이 없다.
* 모바일과 1366×768 화면에서 레이아웃이 깨지지 않는다.
* lint, typecheck, build, 핵심 테스트가 통과한다.
