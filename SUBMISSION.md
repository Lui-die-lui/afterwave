# 제출용 텍스트 초안 (CHECK.md 제출물 항목)

이 파일은 커밋용 문서가 아니라, 과제 제출 플랫폼에 붙여넣을 텍스트를 준비해둔 초안입니다.
`[ ]`로 표시한 부분은 실제 배포 후 값으로 채워야 합니다.

## 결과물 / 소스 주소

- 공개 심사 화면: `[Vercel 배포 URL]`
- 소스 저장소: https://github.com/Lui-die-lui/afterwave

## 짧은 확인 방법 4줄 (T04-C27)

① 어디로 가나요 — 위 공개 심사 화면 URL로 접속합니다. 로그인·회원가입·초대 없이 새 시크릿 창에서 바로 열립니다.
② 3단계 이내 무엇을 하나요 — (1) 첫 화면에서 현재 최대 규모·단위·장소·깊이·좌표·발생/조회 시각·기준 시간대를 확인 → (2) 하단 "실패 실험실"에서 다섯 실패 버튼(느린 응답/401·403/429/오프라인/형식 변경)을 하나씩 눌러본다 → (3) "Recover D2" 버튼을 누른다.
③ 무엇이 보이면 통과인가요 — 다섯 버튼마다 서로 다른 제목·설명·행동 문구가 뜨고, 실패 후에도 마지막 정상값이 "오래된 값(stale)" 표시와 함께 지워지지 않고 남아 있으며, Recover D2 이후 상태가 다시 "최신(fresh)"·오류 없음으로 돌아오고 합성 저장소에 다음 날짜 기록이 정확히 한 건 추가됩니다.
④ 안 될 때 무엇이 보이나요 — 실패 상태에서는 원인이 다른 문구로 표시되고 "다시 시도" 버튼이 보입니다. 임의의 0·`-`·빈 값이 실제 값처럼 표시되는 일은 없습니다. (Failure Lab 자체가 Supabase 마이그레이션 미실행 등으로 저장소에 연결하지 못하면, 화면이 깨지는 대신 "합성 상태 저장소에 연결하지 못했습니다" 오류 문구가 보입니다.)

## AI와 나의 판단 3줄 (T04-C28) — 초안, 제출 전 직접 검토·수정 필요

이 항목은 학생 본인의 판단을 묻는 항목이라 AI가 대신 확정할 수 없습니다. 아래는 이번 세션의 실제 작업
이력을 바탕으로 한 초안이며, 본인의 실제 경험과 다르면 꼭 고쳐서 제출하세요.

① AI에게 맡긴 일 — USGS 연동·정규화·KST 날짜 키 계산, Supabase 실제/합성 저장소 구현과 분리, 지도
pan/zoom·세이프 에어리어 수학, 패널 열고 닫힘 애니메이션 상태 머신, 다섯 실패 시나리오와 Recover D2
합성 fixture, 보안 경계(비밀키 미노출) 자동 테스트 작성까지 구현 대부분을 AI에게 맡겼습니다.
② 직접 판단한 일 — [예: 실제 기록이 아직 1건뿐인 상황에서 두 번째 기록을 조작하지 않고 정직하게 대기
상태로 유지하기로 한 결정, 전체적인 디자인 방향(아이보리+테라코타 브랜드, 위젯 배치), 어떤 버그를 먼저
고칠지와 언제 배포할지의 우선순위 — 본인이 실제로 판단한 다른 내용으로 바꿔서 채워주세요.]
③ AI 제안을 따르지 않은 일 — [실제로 거절하거나 되돌린 제안이 있었다면 그 내용을 적고, 없었다면
"없음 — 매 단계에서 AI의 제안을 검토한 뒤 그대로 받아들였다" 등 없었던 이유를 적어주세요.]

## 완주 체크리스트 (CHECK.md 기준 자체 점검)

- [x] 실제 공개 원천의 값과 맥락이 보입니다. (USGS GeoJSON, 값/단위/출처/두 시각/기준 시간대 모두 화면에 표시)
- [x] 외부 실패 다섯 종류를 합성 재생합니다. (느린 응답/401·403/429/오프라인/형식 변경 — Failure Lab)
- [x] 마지막 정상값과 일별 기록을 보존합니다. (실패해도 stale 표시로 유지, 0/`-` 임의값 없음)
- [ ] 서로 다른 KST 날짜의 실제 기록 2건과 변화값을 대조합니다. — **아직 1건뿐**, 다음 실제 KST 날짜에
      성공 조회가 한 번 더 필요합니다 (README "실제 일별 기록 2건에 대한 메모" 참고). 기다리는 시간은
      과제의 6~7시간 작업 시간에 포함되지 않는다고 CHECK.md에 명시되어 있습니다.
- [x] 개인정보·비밀값 없이 확인법을 제출합니다. (비밀키는 서버 환경변수 전용, 브라우저·응답·Git 기록에 없음 — 자동 테스트로 검증)
